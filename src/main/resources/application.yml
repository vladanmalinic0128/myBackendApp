server:
  port: 9000
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ipdatabase
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update     #da ostanu podaci u bazi
      #ddl-auto: create-drop      #da se uklone svi podaci iz baze
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect #MySQL8Dialect
  mail:
    #maildev server
    #host: 127.0.0.1
    #port: 1025
    #username: hello
    #password: hello

    #Stvarni mejl
    host: smtp.gmail.com
    port: 587
    username: vladan.malinic@student.etf.unibl.org
    password: unuetuorgmkbfmey

    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true
          connection-timeout: 5000
          timeout: 3000
          write timeout: 5000